{
  "name": "mastra-requestcontext-authtoken-span-leak",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Minimal reproduction: Mastra built-in auth's raw bearer token (mastra__authToken) and user object leak onto every span's requestContext, bypassing the default SensitiveDataFilter.",
  "scripts": {
    "start": "tsx src/reproduce.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mastra/core": "1.55.0",
    "@mastra/observability": "1.16.3",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
